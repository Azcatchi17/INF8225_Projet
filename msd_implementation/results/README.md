# MSD Results

Small, publication-facing result tables for MSD experiments can be kept
here when they are intended to be versioned. Large masks, checkpoints,
patch datasets, and raw model outputs should stay in gitignored runtime
folders such as `data/`, `work_dirs/`, or Drive.
