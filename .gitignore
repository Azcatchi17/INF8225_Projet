# =====================================================================
# Heavy assets and runtime artefacts
# =====================================================================

# Raw and processed datasets — symlinked from Drive in Colab.
# We track only the small CSVs in data/results/.
data/raw/
data/processed/
data/MSD_pancreas/
data/Kvasir-SEG/
data/classifier_dataset_*/
data/outputs/
data/outputs_*/

# Trained model checkpoints
models/
*.pth
*.pth.tar
*.ckpt
*.safetensors

# Detection / segmentation work directories
work_dirs/

# Run outputs (logs, intermediate dumps)
outputs/

# MedSAM submodule binaries are not committed
MedSAM/work_dir/MedSAM/medsam_vit_b.pth

# =====================================================================
# Calibration artefacts (regenerated by the pipelines)
# =====================================================================

optimal_threshold*.txt
optimal_threshold*.json
threshold_*.txt
threshold_*.json

# =====================================================================
# Build / Python
# =====================================================================

__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
build/
dist/
.installed.cfg
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Virtual envs
.venv/
venv/
env/
.env

# =====================================================================
# Jupyter
# =====================================================================

.ipynb_checkpoints/
*-checkpoint.ipynb

# =====================================================================
# LaTeX intermediate files
# =====================================================================

report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.fls
report/*.fdb_latexmk
report/*.synctex.gz
report/*.bbl
report/*.blg
report/*.run.xml
rapport/*.aux
rapport/*.log
rapport/*.out
rapport/*.toc
rapport/*.fls
rapport/*.fdb_latexmk
rapport/*.synctex.gz
rapport/*.bbl
rapport/*.blg

# =====================================================================
# Editor / OS
# =====================================================================

.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~
