# Minimal deps for running the project notebooks on Google Colab.
# Colab already ships torch, torchvision, numpy, pandas, matplotlib,
# scikit-image, scikit-learn, opencv-python, Pillow, tqdm.
# mmengine / mmcv / mmdet are installed separately in notebooks/colab_setup.py because
# their wheels must match Colab's exact torch+cuda+python combo.
#
# transformers>=4.38 is the first release with cp312 wheels (via tokenizers>=0.15)
# and still works with mmdet 3.3.0's Grounding DINO (BertModel/BertTokenizer).
transformers>=4.38.0,<4.46.0
nltk
pycocotools
fairscale
addict
yapf
shapely
terminaltables
# Gemini-assisted segmentation pipeline
google-genai>=0.3.0
pydantic>=2.5.0
